# hosts:
  remote_user: ec2-user
  become: yes
  roles:
    initialize-common-server

hosts: jenkins-master
  # remote_user: provision
  # become: yes
  # roles:
    # initialize-jenkins-master

# hosts: jenkins-workers
  # 
  # roles:
    # initialize-jenkins-worker

